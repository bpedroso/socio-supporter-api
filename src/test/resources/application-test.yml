eureka:
  client:
    enabled: false

security:
  basic:
    enabled: false
  management:
    security:
      enabled: false

api:
  saas:
    path: /solr/master_carrefour_Product/select
    querystring:
      wt: json
      q: "*:*"
      fqdate: creationtime_date:[query_date TO *]

feign:
  product:
    name: SAAS
    url: http://localhost:5551